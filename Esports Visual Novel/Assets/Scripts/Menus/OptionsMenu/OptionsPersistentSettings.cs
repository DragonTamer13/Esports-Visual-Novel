using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// Saves settings set in the options menu between scenes. Sets Writer properties on new scene load.
/// </summary>
public class OptionsPersistentSettings : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
